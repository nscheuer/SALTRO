Flight Computer Usage
=====================

SALTRO should be deployed on a Linux-capable flight computer (OBC) with a 64-bit CPU and hardware floating-point support. Build from source uses CMake and a C++17 copmiler, typically via cross-compilation on the ground. Onboard runtime does not require CMake if prebuilt binaries are deployed. 

